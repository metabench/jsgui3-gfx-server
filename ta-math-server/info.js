const {info} = require('jsgui3-gfx').ta_math;
const {overlapping_bounds, byi_from_cs_pos, get_instance} = info;

module.exports = {
    overlapping_bounds: overlapping_bounds,
    byi_from_cs_pos: byi_from_cs_pos,
    get_instance: get_instance
}